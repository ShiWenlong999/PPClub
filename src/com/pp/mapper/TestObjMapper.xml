<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pp.mapper.TestObjMapper" >
	<select id="getTestObj" parameterType="Integer" resultType="com.pp.po.TestObj">
		select * from ptest where id=#{id}
	</select>
</mapper>